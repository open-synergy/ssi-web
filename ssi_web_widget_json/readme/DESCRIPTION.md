A field widget (`json_viewer`) that renders a `Text` or `Char` field
containing JSON as an interactive, collapsible tree when the field is
read-only, and as a syntax-validated textarea when it is editable.
