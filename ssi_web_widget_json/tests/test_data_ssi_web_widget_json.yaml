scenarios:
  - name: "ssi_web_widget_json - Module is installed"
    steps:
      - step: "Search for installed module"
        action: "search"
        model: "ir.module.module"
        domain:
          - ["name", "=", "ssi_web_widget_json"]
          - ["state", "=", "installed"]
        save_as: "module"
        expect_count: 1
