A field widget (`rjsf`) that lets users edit a `json` field using
[React JSON Schema Form (RJSF)](https://rjsf-team.github.io/react-jsonschema-form/)
in edit mode. In read-only mode the stored value is displayed as a
formatted JSON block.
