Registers a custom `ssi_gantt` view type. It renders one horizontal bar per
record, positioned and sized according to a start date field and either a
stop date field or a delay (in hours) field declared on the view
architecture, with records optionally grouped into rows by a
`default_group_by` field.
