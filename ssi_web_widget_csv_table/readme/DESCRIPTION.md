A field widget (`csv_table`) that renders a `Text` field containing CSV
data as a paginated HTML table, with the first row treated as the header.
While editable, it offers a toggle between a raw CSV textarea and a table
view whose cells can be edited directly.
